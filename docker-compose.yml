services:
  app:
    image: ghcr.io/Yougahei/bytebroad-canvas:latest
    build:
      context: .
      dockerfile: Dockerfile
    pull_policy: always
    container_name: bytebroad-canvas
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    ports:
      - "3000:3000"
    restart: unless-stopped
